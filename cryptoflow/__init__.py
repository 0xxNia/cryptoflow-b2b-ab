"""
CryptoFlow — growth-focused experimentation and analytics for crypto exchanges.

Public surface (import from submodules as needed):
  cryptoflow.stats          — CUPED, mSPRT, Bayesian expected loss
  cryptoflow.assignment     — deterministic bucketing
  cryptoflow.regimes        — market regime tagging
  cryptoflow.experiments    — experiment / exposure models
  cryptoflow.metrics        — metric catalog
  cryptoflow.export         — Amplitude-style payloads
  cryptoflow.pipeline       — exposure log contracts
  cryptoflow.integrity      — AA/SRM integrity guardrails
"""

__version__ = "0.1.0"
